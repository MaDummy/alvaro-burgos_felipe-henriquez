#ifndef UTILS_H
#define UTILS_H

/// @brief Escribe el archivo con el hashmap especificado
/// @param ruta Ruta del archivo en el que se escribirá
void escribe_hashmap(const char const* ruta);

#endif